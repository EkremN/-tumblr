
<div class="container">
  <div class="row">
    <h1 class="mt-3"> <%= @post.title %></h1>
    <p> Submitted: <%= time_ago_in_words(@post.created_at) %></p>
      <p> <%= @post.body %> </p>
  </div>
</div>
